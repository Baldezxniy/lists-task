server.port: 8080

logging:
  level:
    root: INFO
    com.example: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tasklist_dev?currentSchema=tasklist
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: classpath:liquibase/db.changelog-master.yaml
    enabled: true
    default-schema: tasklist
    url: jdbc:postgresql://localhost:5432/tasklist_dev?currentSchema=tasklist
    user: postgres
    password: password

mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml

security:
  jwt:
    secret: SVVHT1dVSVV5ZWdmZ3J2cWtld2hqYmZsaVVCQUdP
    access: 1
    refresh: 30

springdoc:
  override-with-generic-response: false

---
spring.config.activate.on-profile: docker

# DOCKER CONFIG