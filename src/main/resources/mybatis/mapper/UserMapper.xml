<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.tasklist.repositories.UserRepository">
    <resultMap id="UserResultMap" type="com.example.tasklist.domain.user.User" autoMapping="false">
        <id property="userId" column="user_id"/>
        <result property="name" column="user_name"/>
        <result property="username" column="user_username"/>
        <result property="password" column="user_password"/>
        <collection property="roles" ofType="com.example.tasklist.domain.user.Role" autoMapping="false">
            <result property="name" column="user_role_role"/>
        </collection>
        <collection property="tasks" ofType="com.example.tasklist.domain.task.Task"
                    resultMap="com.example.tasklist.repositories.TaskRepository.TaskResultMap" autoMapping="false"/>
    </resultMap>

    <select id="findById" resultMap="UserResultMap">
        SELECT u.user_id
        as user_id,
        u.name            as user_name,
        u.username        as user_username,
        u.password        as user_password,
        ur.role           as user_role_role,
        t.task_id         as task_id,
        t.title           as task_title,
        t.description     as task_description,
        t.expiration_date as task_expiration_date,
        t.status          as task_status
        FROM users u
        LEFT JOIN users_roles ur on u.user_id = ur.user_id
        LEFT JOIN users_tasks ut on u.user_id = ut.user_id
        LEFT JOIN tasks t on ut.task_id = t.task_id
        WHERE u.user_id = #{userId}
    </select>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT u.user_id
        as user_id,
        u.name            as user_name,
        u.username        as user_username,
        u.password        as user_password,
        ur.role           as user_role_role,
        t.task_id         as task_id,
        t.title           as task_title,
        t.description     as task_description,
        t.expiration_date as task_expiration_date,
        t.status          as task_status
        FROM users u
        LEFT JOIN users_roles ur on u.user_id = ur.user_id
        LEFT JOIN users_tasks ut on u.user_id = ut.user_id
        LEFT JOIN tasks t on ut.task_id = t.task_id
        WHERE u.username = #{username}
    </select>

    <update id="update">
        UPDATE users
        SET
        name = #{name},
        username = #{username},
        password = #{password}
        WHERE user_id = #{userId}
    </update>

    <insert id="create" keyProperty="task_id" keyColumn="task_id" useGeneratedKeys="true">
        INSERT INTO users (name, username, password)
        VALUES (#{name}, #{username}, #{password})
    </insert>

    <insert id="insertUserRole">
        INSERT INTO users_roles (user_id, role)
        VALUES (#{userId}, #{role})
    </insert>

    <select id="isTaskOwner" resultType="boolean">
        SELECT exists(
            SELECT 1 FROM users_tasks
            WHERE user_id = ${userId}
            AND task_id = #{taskId}
        )
    </select>

    <delete id="delete">
        DELETE FROM users
        WHERE user_id = #{userId}
    </delete>
</mapper>